<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="dialog_base_style" parent="AppTheme">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowAnimationStyle">@style/Animation.AppCompat.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustResize</item>
    </style>

    <style name="dialog_transparency_style" parent="dialog_base_style">
        <item name="android:windowBackground">@color/transparency</item>
    </style>

    <style name="style_dialog_bottom" parent="dialog_transparency_style">
        <item name="android:windowBackground">@color/transparency</item>
    </style>

    <style name="style_dialog_center" parent="dialog_transparency_style">
        <item name="android:windowBackground">@color/transparency</item>
    </style>

    <style name="style_dialog_top" parent="dialog_transparency_style">
        <item name="android:windowBackground">@color/transparency</item>
    </style>

    <style name="style_dialog_wrap" parent="Theme.AppCompat.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
    <!--***********************************************使用下面的主题***************************************-->
    <style name="AppCompatDialog.Base" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowIsFloating">true</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFrame">@null</item>

        <item name="listPreferredItemPaddingLeft">0dp</item>
        <item name="listPreferredItemPaddingRight">0dp</item>
    </style>

    <style name="Dialog.Base" parent="@android:style/Theme.Dialog">
        <!-- Dialog的windowFrame框为无 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否漂现在activity上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 去除黑色边框的关键设置项 -->
        <item name="android:windowContentOverlay">@null</item>
        <!-- 屏幕背景是否变暗 -->
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>

    </style>

    <style name="Dialog.BottomSheet" parent="Dialog.Base">
        <!--为了避免在有 NavigationBar 的手机上 Dialog 从 NavigationBar 底部上来。
      去掉 Dialog 的动画，使用 View 的动画-->
        <item name="android:windowAnimationStyle">@null</item>
    </style>

</resources>